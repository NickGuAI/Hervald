# Issue Triage

Investigate why the chat transcript drops messages after a provider reconnect. Read the session runtime, conversation store, frontend transcript hook, and tests. Return root cause, a fix plan, and the guardrail test that would prevent regression.
